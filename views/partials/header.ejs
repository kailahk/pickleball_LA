<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pickleball_LA</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
<nav>
    <a href="/" <%- title === 'Home Page' ? 'class="active"' : '' %>>Home Page</a>
    <% if (user) { %>
      <a href=""    <%- title === 'Courts' ? 'class="active"' : '' %>>
          Courts</a>
      <a href="" <%- title === 'Reservations' ? 'class="active"' : '' %>>Reservations</a>
      <div class="logout">
        <a href="/logout">LOG OUT: </a>
        <p><%- user.name %></p>
        <img src="<%- user.avatar %>" alt="avatar" referrerpolicy="no-referrer">
      </div>
    <% } else { %>
      <div class="loginLink">
        <a href="/auth/google" class="login">LOG IN&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"></a>
    <% } %>
      </div>
  </nav>
  <main>
    <h1><%= title %></h1>
    
